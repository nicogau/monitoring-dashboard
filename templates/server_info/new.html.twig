{% extends 'base.html.twig' %}

{% block title %}ajouter un serveur{% endblock %}

{% block body %}
    <div class="container d-flex justify-content-center ">
        <div class="mb-3">
            <h3>Ajouter un serveur</h3>
            {{ include('server_info/_form.html.twig') }}

            <div class="d-flex justify-content-center">
                <a class="btn btn-primary" href="{{ path('app_info') }}">retour à la liste</a>
            </div>
        </div>
    </div>

{% endblock %}
