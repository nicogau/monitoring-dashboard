<form method="post" action="{{ path('app_server_info_delete', {'id': server_info.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ server_info.id) }}">
    <button class="btn text-danger">supprimer</button>
</form>
