{% extends 'base.html.twig' %}

{% block title %}modifier un Vhost {% endblock %}

{% block body %}

    <div class="container d-flex justify-content-center ">
        <div class="mb-3">
          <h1>modifier un Vhost</h1>
          {{ include('vhost/_form.html.twig', {'button_label': 'modifier'}) }}
          <div class="d-flex flex-column justify-content-center  align-items-center">
              <a class=" btn btn-primary my-1" href="{{ path('app_vhost_index') }}">retour à la liste</a>
              <div>
                  {{ include('vhost/_delete_form.html.twig') }}
              </div>
          </div>
        </div>
    </div>


{% endblock %}
