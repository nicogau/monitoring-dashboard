<nav class="navbar navbar-expand-lg bg-light">
    <div class="container-fluid">
        {# <a class="navbar-brand" href="#">Navbar</a> #}
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
            {# lien page listant toutes les infos #}
            <li class="nav-item">
                <a class="nav-link" href="{{ path('app_home')}}">infos</a>
            </li>
            {# lien page configuration virtual host #}
            <li class="nav-item">
                <a class="nav-link" href="{{ path('app_vhost')}}">vhost</a>
            </li>
            {# lien page configuration serveur #}
            <li class="nav-item">
                <a class="nav-link" href="{{ path('app_server')}}">serveur</a>
            </li>
            {# lien page test #}
            <li class="nav-item">
                <a class="nav-link" href="{{ path('app_test')}}">test</a>
            </li>
        </ul>
        </div>
    </div>
</nav>